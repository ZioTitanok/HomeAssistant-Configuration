# Recorder
db_url: !secret mariadb_url
exclude:
  domains:
    - automation
    - input_boolean
    - input_datetime
    - input_select
    - script
  entities:
    - sensor.date
    - sensor.time
    - sensor.time_utc
    - sensor.season

include:
  domains:
    - binary_sensor
    - climate
    - device_tracker
    - light
    - media_player
    - person
    - plant
    - sensor
    - switch
  entities:
    - automation.main_gate_management
    - automation.main_gate_nfc_clio
    - automation.main_gate_nfc_ypsilon
    - climate.loft
    - climate.main
    - input_boolean.loft_discoball_activate
    - input_boolean.radio_activate
    - input_datetime.loft_auxiliary_on
    - input_datetime.loft_auxiliary_off
    - input_number.night
    - input_number.radio_volume
    - vacuum.roomba
    - sun.sun
